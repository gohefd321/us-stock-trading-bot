# Backend API URL
# For local development: http://localhost:8000
# For Cloudflare Tunnel: https://stocking.daechanserver.com (same domain as frontend)
# Leave empty to use relative URLs (recommended for same-domain deployment)
VITE_API_URL=
